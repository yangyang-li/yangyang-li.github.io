<style>
  :root {
    --yangyang-accent: #2f6b5f;
    --yangyang-paper: #fbfaf6;
    --yangyang-ink: #202825;
  }

  html,
  body {
    font-family: "Roboto", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  body {
    color: var(--yangyang-ink);
    background-color: var(--yangyang-paper);
  }

  a,
  .navbar-brand,
  .nav-link.active {
    color: var(--yangyang-accent);
  }

  .home-hero {
    position: relative;
    isolation: isolate;
    margin: 1.5rem 0 2.75rem;
    padding: clamp(2rem, 6vw, 4.75rem) clamp(1.5rem, 6vw, 4.5rem);
    overflow: hidden;
    border: 1px solid rgba(47, 107, 95, 0.12);
    border-radius: 1.25rem;
    background:
      linear-gradient(90deg, rgba(251, 250, 246, 0.96) 0%, rgba(251, 250, 246, 0.84) 52%, rgba(251, 250, 246, 0.48) 100%),
      url("{{ '/assets/img/shanshui-hero.png' | relative_url }}") center / cover no-repeat;
    box-shadow: 0 1.25rem 3rem rgba(42, 55, 50, 0.07);
  }

  .home-hero p {
    max-width: 47rem;
    font-size: clamp(1rem, 1.6vw, 1.13rem);
    line-height: 1.8;
  }

  h2 {
    margin-top: 2.4rem;
    letter-spacing: -0.015em;
  }

  .publications h2 {
    color: var(--yangyang-accent);
  }

  .chinese-page,
  .chinese-page h1,
  .chinese-page h2,
  .chinese-page h3,
  .chinese-page h4 {
    font-family: "Noto Serif SC", "Songti SC", "STSong", SimSun, serif;
  }

  .chinese-page {
    line-height: 1.95;
    text-align: justify;
  }

  .chinese-page h2 {
    padding-bottom: 0.35rem;
    border-bottom: 1px solid rgba(47, 107, 95, 0.18);
  }

  @media (prefers-color-scheme: dark) {
    :root {
      --yangyang-accent: #78b6a7;
      --yangyang-paper: #171b1a;
      --yangyang-ink: #edf0ed;
    }

    .home-hero {
      border-color: rgba(120, 182, 167, 0.22);
      background:
        linear-gradient(90deg, rgba(23, 27, 26, 0.96) 0%, rgba(23, 27, 26, 0.88) 58%, rgba(23, 27, 26, 0.68) 100%),
        url("{{ '/assets/img/shanshui-hero.png' | relative_url }}") center / cover no-repeat;
    }
  }

  @media (max-width: 720px) {
    .home-hero {
      margin-top: 1rem;
      background-position: 62% center;
    }
  }
</style>
